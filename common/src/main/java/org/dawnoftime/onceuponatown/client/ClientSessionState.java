package org.dawnoftime.onceuponatown.client;

public class ClientSessionState {
    // Resets to null on game launch / world load (not serialized).
    public static String selectedEraPathId = null;
}
